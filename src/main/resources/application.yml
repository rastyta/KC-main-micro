spring:
  application:
    name: kcmainmicro
  cloud:
    kubernetes:
      discovery:
        enabled: true
      reload:
        enabled: true
        monitoring-config-maps: true
        strategy: refresh
        mode: event
      config:
        name: kcmainmicro

logging:
  level:
    org.springframework.cloud.kubernetes: DEBUG