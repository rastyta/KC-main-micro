spring:
  application:
    name: kcmainmicro
  cloud:
    kubernetes:
      discovery:
        enabled: true
      reload:
        enabled: true
      config:
        enabled: true
        name: kcmainmicro
logging:
  level:
    org.springframework.cloud.kubernetes: DEBUG